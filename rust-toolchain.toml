[toolchain]
# channel = "beta" # 1.95.0-beta.3 broken
# channel = "nightly-2026-03-13" # broken
# channel = "nightly-2026-06-01" # broken
channel = "nightly-2026-06-01"
components = ["rustfmt", "clippy", "rust-analyzer"]
profile = "minimal"

